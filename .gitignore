# OS / editor
.DS_Store
*.swp
.idea/
.vscode/

# Env / secrets
.env
.env.*
!.env.example

# Python (provisional — confirm stack during domain modeling)
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Jupyter
.ipynb_checkpoints/

# Build / data artifacts
dist/
build/
*.egg-info/

# Raw Volve production export — calibration source, not vendored (issue #13; see spec/volve/README.md)
spec/volve/*.xlsx

# Sealed round-2 change-request contents (#24, ADR 0015): held out of version control until round
# close; only the manifest's seal.digest is committed pre-tag. Release = `git add -f` this dir.
spec/contest/change-requests/.sealed/

# Sealed adversarial paraphrase variants (#51, ADR 0015): held out of version control until released
# in the eval-bundle feed; only the manifest's seal.digest is committed pre-tag. Release = `git add -f`.
spec/questions/adversarial-variants/.sealed/

